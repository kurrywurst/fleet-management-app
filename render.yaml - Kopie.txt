services:
  - type: web
    name: fleet-management-backend
    env: python
    buildCommand: "cd fleet-management-backend && pip install -r requirements.txt"
    startCommand: "cd fleet-management-backend && python src/main.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: FLASK_ENV
        value: production

  - type: web
    name: fleet-management-frontend
    env: node
    buildCommand: "cd fleet-management-frontend && pnpm install && pnpm run build"
    startCommand: "cd fleet-management-frontend && pnpm run preview"
    envVars:
      - key: NODE_VERSION
        value: 20
